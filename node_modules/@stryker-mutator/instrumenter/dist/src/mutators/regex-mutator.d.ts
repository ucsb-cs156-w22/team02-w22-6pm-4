import { NodePath } from '@babel/core';
import * as weaponRegex from 'weapon-regex';
import { NodeMutation } from '../mutant';
import { NodeMutator } from '.';
export declare class RegexMutator implements NodeMutator {
    private readonly weaponRegexMutateImpl;
    name: string;
    constructor(weaponRegexMutateImpl?: typeof weaponRegex.mutate);
    mutate(path: NodePath): NodeMutation[];
    private mutatePattern;
}
//# sourceMappingURL=regex-mutator.d.ts.map